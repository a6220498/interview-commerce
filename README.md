# Interview Commerce Project - 微型電商實作

這是一個基於 **Vue 2** 與 **Vite** 開發的微型電商前端專案。專案重點在於展示高品質的 UI/UX 互動、模組化的 CSS 架構以及效能優化的動畫技術，專為面試技術展示而設計。

## 🚀 技術棧 (Tech Stack)

- **Framework**: Vue 2.6.14
- **State Management**: Vuex 3.6.2 (購物車邏輯、全域狀態管理)
- **Routing**: Vue Router 3.6.5
- **Build Tool**: Vite 4.5.14
- **Styling**: SCSS (Dart Sass), CSS Variables
- **Design Concepts**: Modular SCSS (@use), Safe Area Support

## ✨ 核心功能 (Key Features)

- **動態商品呈現**: 響應式商品網格系統，支援裝置差異化互動。
- **高效能飛入動畫**: 透過 CSS `transform` 與 `requestAnimationFrame` 實現流暢的「飛入購物車」視覺效果，避免造成瀏覽器像素重排（Reflow）。
- **完善購物車流程**: 
  - 動態增刪品項。
  - 自動計算總價與數量。
  - 結帳成功跳轉與狀態清理。
- **安全區域適配 (Safe Area)**: 完整支援 iOS 裝置的 Notch 與 Home Indicator，確保全螢幕沈浸式體驗。

## 🛠 技術亮點 (Technical Highlights)

### 1. 模組化 SCSS 結構
專案採用現代 SASS 的 `@use` 語法，將樣式拆分為獨立模組（Variables, Base, Buttons, Animations），大幅提升樣式的可維護性並避免命名衝突。

### 2. UI/UX 效能優化
- **動畫技術**: 購物車飛入效果僅使用 `translate` 與 `scale` 進行變換，確保即便在低階行動裝置上也能維持 60 FPS 的動畫流暢度。
- **RWD 策略**: 
  - 桌面版：滑鼠懸浮（Hover）觸發操作按鈕，增強直覺。
  - 行動版：單次點擊（Tap）即刻進入詳情，優化行動端反應速度。

### 3. 全面適配行動端
- 整合 `viewport-fit=cover` 與 `env(safe-area-inset)`，確保 UI 內容在不同屏佔比的手機上都能正確對齊。

## 📦 安裝與啟動 (Getting Started)

### 前置要求
- Node.js (推薦 v18.0.0+)
- pnpm / npm / yarn

### 安裝依賴
```bash
pnpm install
```

### 開發環境啟動
```bash
pnpm run dev
```

### 專案打包
```bash
pnpm run build
```

---

## 📂 專案結構 (Project Structure)
```text
src/
├── assets/
│   ├── scss/           # 模組化 SCSS (Variables, Base, etc.)
│   └── styles.scss     # 全域樣式入口
├── components/         # 共用 UI 組件 (AddToCartDialog, ProductCard, etc.)
├── data/               # 靜態模擬數據
├── router/             # 路由配置
├── store/              # Vuex 狀態管理
└── views/              # 頁面組件 (Home, Cart, Success)
```
